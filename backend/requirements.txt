# Web framework
fastapi>=0.110
uvicorn[standard]>=0.29

# Validation / settings
pydantic>=2.7
pydantic-settings>=2.2

# Database
SQLAlchemy>=2.0.30
psycopg[binary]>=3.1
# Schema migrations
alembic>=1.13

# LLM (Claude / Anthropic) — latest recommended for messages.parse + Opus 4.8
anthropic>=0.40

# HTTP client (Tavily search)
httpx>=0.27

# Model Context Protocol — topic-retrieval MCP server + client used by Agent 1
mcp>=1.2

# Text to speech
google-cloud-texttospeech>=2.16

# Multipart (FastAPI form support)
python-multipart>=0.0.9
