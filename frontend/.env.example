# Base URL for the backend API.
# In dev, leave unset — Vite proxies /api to http://localhost:8000 (see vite.config.ts).
# Set this when serving the built app from a different origin than the API.
# VITE_API_BASE=https://api.example.com/api
